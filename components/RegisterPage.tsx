import ProfileSetup from './ProfileSetup';

// Minimal compatibility shim: export ProfileSetup as the default and also named RegisterPage.
export { ProfileSetup as RegisterPage };
export default ProfileSetup;